# Project Instructions

## Workflow
- **Continuous Deployment:** Always publish updates to GitHub (`git push origin main`) after successful implementation and validation of changes. This triggers the automatic deployment to Vercel.
- **Commit Style:** Use descriptive prefix-based commit messages (e.g., `feat:`, `fix:`, `refactor:`).
